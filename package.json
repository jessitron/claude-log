{
  "name": "claude-log",
  "version": "1.0.0",
  "description": "This project analyzes Claude Code conversation logs to understand Claude's activity during conversations.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "parse": "tsc && node dist/main.js",
    "explore": "tsc && node dist/explore.js",
    "show": "tsc && node dist/show.js",
    "trace": "tsc && node dist/trace.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2"
  }
}
